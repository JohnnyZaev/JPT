using VContainer;
using VContainer.Unity;

namespace JPT.Scripts.Runtime.Bootstrap
{
    public class BootstrapScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            base.Configure(builder);
        }
    }
}
